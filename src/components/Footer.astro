---
import { navItems } from '@data/nav';
import logoBlanco from '@assets/logos/logo-blanco.svg?raw';

const currentYear = new Date().getFullYear();

const categoryLinks = [
  { label: 'Comerciales', href: '#portafolio' },
  { label: 'Residenciales', href: '#portafolio' },
  { label: 'Industriales', href: '#portafolio' },
  { label: 'Desarrollo nacional', href: '#portafolio' },
];

const serviceLinks = [
  { label: 'Remodelación', href: '#servicios' },
  { label: 'Construcción', href: '#servicios' },
  { label: 'Consultoría', href: '#servicios' },
  { label: 'Cálculo Estructural', href: '#servicios' },
];
---
<footer class="site-footer section-dark">
  <div class="container">
    <div class="footer-top">
      <span class="footer-stamp">Corpoha · 2026</span>
      <span class="footer-coords">10°30′N 66°54′W</span>
    </div>

    <div class="footer-grid">
      <div class="footer-brand">
        <span class="footer-logo" set:html={logoBlanco} />
        <p class="footer-tagline">
          Construcción y remodelación<br>
          Caracas · Venezuela
        </p>
        <a href="https://instagram.com/corpoha85" rel="noopener" target="_blank" class="footer-social">
          @corpoha85
        </a>
      </div>

      <div class="footer-column">
        <h4><span class="footer-column-num">01</span>Navegación</h4>
        <ul>
          {navItems.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </div>

      <div class="footer-column">
        <h4><span class="footer-column-num">02</span>Servicios</h4>
        <ul>
          {serviceLinks.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>

      <div class="footer-column">
        <h4><span class="footer-column-num">03</span>Obras</h4>
        <ul>
          {categoryLinks.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>

      <div class="footer-column">
        <h4><span class="footer-column-num">04</span>Legal</h4>
        <ul>
          <li><a href="/politica-privacidad">Política de privacidad</a></li>
          <li><a href="/terminos">Términos</a></li>
        </ul>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; {currentYear} Corporación HA. Todos los derechos reservados.</p>
      <p class="footer-pending">RIF pendiente de confirmación</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    padding-block: var(--space-16) var(--space-12);
  }

  .footer-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: var(--space-10);
    border-bottom: 1px solid color-mix(in oklch, var(--text-on-dark) 20%, transparent);
  }

  .footer-stamp {
    font-family: var(--font-heading);
    font-size: 0.6875rem;
    font-weight: 600;
    color: var(--action-primary);
    letter-spacing: var(--letter-spacing-technical);
    text-transform: uppercase;
    padding-left: var(--space-4);
    border-left: 3px solid var(--action-primary);
  }

  .footer-coords {
    font-family: var(--font-heading);
    font-size: 0.6875rem;
    font-weight: 500;
    color: var(--text-dim-on-dark);
    letter-spacing: var(--letter-spacing-technical);
    text-transform: uppercase;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-12);
    padding-block: var(--space-16);
    border-bottom: 1px solid color-mix(in oklch, var(--text-on-dark) 15%, transparent);
  }

  .footer-brand {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .footer-logo :global(svg) {
    height: 56px;
    width: auto;
    display: block;
  }

  .footer-tagline {
    color: var(--text-dim-on-dark);
    max-width: 260px;
    font-size: var(--fs-body);
    line-height: 1.5;
  }

  .footer-social {
    font-family: var(--font-body);
    font-size: var(--fs-body);
    font-weight: 500;
    color: var(--text-on-dark);
    border-bottom: 1px solid var(--action-primary);
    padding-bottom: 2px;
    align-self: flex-start;
    transition: color 150ms ease-out;
  }

  .footer-social:hover {
    color: var(--action-primary);
  }

  .footer-column h4 {
    display: flex;
    align-items: baseline;
    gap: var(--space-2);
    font-family: var(--font-heading);
    font-size: 0.6875rem;
    font-weight: 600;
    color: var(--text-on-dark);
    letter-spacing: var(--letter-spacing-technical);
    text-transform: uppercase;
    margin-bottom: var(--space-6);
    padding-bottom: var(--space-3);
    border-bottom: 1px solid color-mix(in oklch, var(--text-on-dark) 20%, transparent);
  }

  .footer-column-num {
    font-family: var(--font-display);
    font-weight: 800;
    color: var(--action-primary);
    letter-spacing: -0.02em;
  }

  .footer-column ul {
    display: grid;
    gap: var(--space-3);
  }

  .footer-column a {
    color: var(--text-dim-on-dark);
    font-size: var(--fs-body);
    transition: color 150ms ease-out, padding-left 150ms ease-out;
    display: inline-block;
  }

  .footer-column a:hover {
    color: var(--text-on-dark);
    padding-left: var(--space-2);
  }

  .footer-bottom {
    margin-top: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    font-family: var(--font-heading);
    font-size: 0.6875rem;
    font-weight: 500;
    color: var(--text-dim-on-dark);
    letter-spacing: var(--letter-spacing-mono);
    text-transform: uppercase;
  }

  .footer-pending {
    opacity: 0.6;
    font-style: italic;
  }

  @media (min-width: 640px) {
    .footer-grid {
      grid-template-columns: 1.5fr 1fr 1fr;
    }

    .footer-brand {
      grid-column: 1 / -1;
    }
  }

  @media (min-width: 1024px) {
    .footer-grid {
      grid-template-columns: 1.8fr repeat(4, 1fr);
    }

    .footer-brand {
      grid-column: auto;
    }

    .footer-bottom {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }
  }
</style>
